{
  "name": "@depot/prisma",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "migrate": "dotenvx run -f ../../.env -- prisma migrate dev --schema=./schema.prisma",
    "reset": "dotenvx run -f ../../.env -- prisma migrate reset --schema=./schema.prisma",
    "generate": "dotenvx run -f ../../.env -- prisma generate --schema=./schema.prisma"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.17.1",
    "prisma": "^6.17.1"
  }
}
